/**
 * Home page (Server Component).
 * Nothing to add here — the dashboard shell lives in the layout.
 * Returning children passes control straight to the ClientShell.
 */
export default function Home() {
  return null;
}
